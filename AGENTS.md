# Beyond: Craftlines 多版本约束

- 所有开发工作必须在开发分支上进行，不得直接在 `main` 分支修改或提交；开发内容完成并提交后，必须立即推送到对应远端开发分支。
- 本项目同时支持 Minecraft 1.20.1 Forge、1.21.1 NeoForge 和 26.1.2 NeoForge。
- 用户未明确限定版本时，任何功能修改、修复、配置、协议、资源、文档、测试、构建和打包都必须同时覆盖三个版本。
- 不得把 1.21.1 描述为默认版本或只提供 1.21.1 的构建/产物说明。单版本说明只能用于真实的版本专用 API、路径或命令，并且同一处必须给出另外两个版本的对应信息。
- 根目录 `src` 是共享实现及 1.21.1 构建源码；`versions/1.20.1` 通过源码转换和版本专用实现适配 Forge；`versions/26.1.2` 保存 26.1.2 专用源码。修改共享逻辑后必须检查两个版本目录是否需要同步或适配。
- 交付前分别使用 JDK 17、21、25 构建 1.20.1、1.21.1、26.1.2，并列出三个 `build/libs` 中本次生成的 jar。
