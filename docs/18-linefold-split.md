# 构象系统拆分说明

自 `0.1.0` 起，本模组只保留 Beyond Dimensions 网络下单、JEI 配方树、机器绑定和合成链供给器。

以下内容已经迁移到独立模组 `Sky Logistics: Linefold`（`sky_logistics_linefold`）：构象框选、结构快照、运行时沙盒、试产计量、报告确认、稳定构象图、构象执行器、构象复制台和有限生产队列。

旧文档中涉及上述构象域的章节仅作为拆分前设计历史，不代表 `beyond_craftlines` 当前注册内容；当前边界以项目 README、源码注册表和模组元数据为准。
